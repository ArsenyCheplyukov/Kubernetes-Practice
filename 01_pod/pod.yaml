apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
  # labels:
  #   app.kubernetes.io/name: my-first-pod
spec:
  containers:
  - name: nginx
    image: nginx:latest
    # resources:
    #   limits:
    #     memory: "128Mi"
    #     cpu: "500m"
    ports:
      - containerPort: 80
